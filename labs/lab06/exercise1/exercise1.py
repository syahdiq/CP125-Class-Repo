def get_legit_power_users(log_data, bot_ids, threshold):

    for bot_ids in log_data:
        if bot_ids == log_data:
            log_data.remove(bot_ids)

        for i in range(len(log_data)):
            if i == i+1:
                count += 1

            if count > threshold:
                pass

    



    
            
   